Import libraries for data handling, computation, and machine learning.
Load the student dataset containing reading, writing, and math scores.
Explore the dataset to check column names and data structure.
Clean column names by removing spaces and special characters.
Select input features (reading and writing scores) and target (math score).
Split the dataset into training (80%) and testing (20%) sets.
Scale the input features to normalize the data.
Train a Linear Regression model using the training data.
Evaluate the model using Mean Squared Error and R² Score.
Predict math scores for new students using trained model.
Display the predicted scores and analyze results.
Conclude that the model can effectively predict student performance.
